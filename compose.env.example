# Copy to .env (do not commit .env)

# Required when tunneling: public HTTPS origin used to build media URLs
# Example: https://mcp.example.com
WHATSAPP_MCP_BASE_URL=https://mcp.example.com

# For cloudflared named tunnel mode
TUNNEL_TOKEN_WHATSAPP=
